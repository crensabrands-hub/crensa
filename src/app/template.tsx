"use client";

import RouteLayoutWrapper from "@/components/layout/RouteLayoutWrapper";

export default function Template({ children }: { children: React.ReactNode }) {
 return <RouteLayoutWrapper>{children}</RouteLayoutWrapper>;
}
